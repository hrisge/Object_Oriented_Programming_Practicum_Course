# OOП - Практикум 12.03.2026 - Седмица 4

# Задача 1.
Да се реализира шаблонна функция void swap(...), която разменя стойностите на две променливи от произволен тип Т.(без std::swap)

# Задача 2. 
Реализирайте темплейтна функция, която сортира подаден масив. Ако масивът е от символи, да се сортира, изпозвайки counting sort, иначе - insertion sort.

# Задача 3. 
Да се реализира шаблонна функция curry, която приема функция на два аргумента и първи аргумент на функцията като връща функция очакваща само втория аргумент.

```
int sum(int a, int b){
	return a+b;
}

...

curry(sum,3)(5) //=8
```

# Задача 4.
Да се реализира функция flip, коят приема функция на два аргумента, и връща нова функция с обърнати аргументи.

```
flip([](int x, int y){return x-y;})(2, 1); // = -1
```

# Задача 5.
Да се напише шаблонна функция void sortBy(T\* arr, size_t size, std::function<bool(const T&)> f), която приема масив от произволен тип и го сортира спрямо някакво свойство на елементите на масива.

```
int arr[5] = {1, 2, 3, 4, 5}
sortBy(arr, [](int x) -> {return x%2;})
//arr = {2,4,1,3,5}
```

# Задача 6.
Да се напише функция T accumulate(std::function<T(const U&, const T&)> op, const T& nv, std::function<U(int)> term, int a, int b), която работи по следния начин:

```
accumulate(op, nv, term, a, b) = (term(a) op (term(a+1) op (... op (term(b) op nv)))...)

//Пример
accumulate(sum, 0, [](int x) -> {return x-1;}, 2, 11) = 55
```
* _Бонус_: Използвайте accumulate със подходящи параметри за да направите фунцкия която връща масив от числата от а до b.



# Задача 7.
Да се реализира шаблонна структура Record<T,K>, която описва един запис. Структурата съдържа ключ K key и T value. Реализирайте и шаблонна структура Collection<T,N>, която пази N на брой брой записи, като ключовете в колекцията трябва да са уникални.

Да се реализират шаблонни функции:
    
 - void readCollection(Collection<T, N>& c, const char* filename) - прочита колекция от текстов файл. Файлът съдържа N реда, всеки със:
    key value
 
 - void writeCollection(const Collection<T, N>& c, const char* filename) - Да записва всички записи.

 - Record<T, K> maxRecord(const Collection<T, N>& c) - Намиране на запис с максимална стойност

 - void sortByValue(Collection<T,N>& c) - Сортиране по стойност (нарастващо)

 - int filter(const Collection<T,N>& c, bool (*pred)(T)) - Филтрира записите и връща броя на останалите записи
 
# Задача 8.
Да се реализира шаблонна структура Vector<T,N>, описваща вектор с най-много N елемента от тип Т. Реализирайте следните функции:

- Vector<U,N> map(std::function<U(const T&)> f, const Vector<T,N>& vec), която връща нов вектор след прилагане на f към всеки от елементите му.
- Vector<T,N> filter(std::function<bool(const T&)> f, const Vector<T,N>& vec), която връща нов вектор, само с елементите за които f(el) е вярно
- bool all(...), която връща дали всички елементи на вектора изпълняват условието f
- bool any(...), която връща дали съществува елемент, който изпълнява условието f
- Vector<V, N> zipWith(std::function<V(const T&, const U&)> op, const Vector<T, N>& vec1, const Vector<V,N>& vec2), връща нов вектор v, като v\[i\] = op(vec1\[i\], vec2\[i\])
